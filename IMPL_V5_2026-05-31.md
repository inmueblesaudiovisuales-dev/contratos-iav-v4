# Contratos v5.0 — Unificación: Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Unificar contratos particulares y estándar en un solo tipo, con propiedades dinámicas, servicios libres y adicionales disponibles para todos.

**Architecture:** Fase 1 — backend primero (no rompe nada en producción), Fase 2 — admin.html (nuevo formulario unificado), Fase 3 — portal.html (quitar guards de tipo). Cada fase se despliega independientemente y se verifica antes de continuar.

**Tech Stack:** Cloudflare Workers (ES modules, V8), D1 (SQLite edge, sin FK), frontend vanilla JS con `var`/`let`/`const` (no Rhino), `wrangler deploy` para despliegue.

**Rutas de los archivos:**
- `worker/src/routes/contratos.js` — `crearContrato`
- `worker/src/routes/portal.js` — `firmaCliente`, `guardarConfiguracion`
- `frontend/admin.html` — formulario de nuevo contrato + JS
- `frontend/portal.html` — eliminar `esParticular`

**Comandos de deploy:** Siempre desde `worker/`:
```bash
cd "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/worker"
npx wrangler deploy
```

---

## Task 0: Pre-deploy — verificar contratos particular en "Pendiente firma"

**Files:** ninguno (solo consulta informativa)

> Si hay contratos `particular` con estatus `Pendiente firma`, tienen tokens de tipo `configurar` que dejarán de funcionar cuando se despliegue el 410. Identificarlos antes de cualquier cambio.

- [x] **Step 1: Abrir Wrangler D1 console**

```bash
cd "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/worker"
npx wrangler d1 execute iav-contratos --remote --command "SELECT token, nombre_cliente, estatus, fecha_creacion FROM contratos WHERE tipo_contrato = 'particular' AND estatus = 'Pendiente firma';"
```

- [x] **Step 2: Interpretar resultado**

Si la consulta devuelve 0 filas: continuar con Task 1.

Si devuelve filas: **no modificar automáticamente** — coordinar con Bruno. Las opciones por contrato son:
- Si el cliente ya nunca va a firmar: ocultarlo desde el admin (botón "Ocultar" en el panel lateral).
- Si el cliente todavía puede firmar: el portal sigue funcionando (el cliente puede llenar dirección y mapas directamente en el formulario de firma). Solo ya no puede usar el flujo de "configurar" previo.
- Si hay duda: dejar el contrato como está — la firma sigue siendo posible aunque `guardarConfiguracion` devuelva 410.

- [x] **Step 3: Confirmar también cuántos particular existen (solo informativo)**

```bash
npx wrangler d1 execute iav-contratos --remote --command "SELECT tipo_contrato, estatus, COUNT(*) as total FROM contratos GROUP BY tipo_contrato, estatus ORDER BY tipo_contrato, estatus;"
```

Guardar el output — sirve de referencia para la verificación post-deploy.

---

## Task 1: Worker — `crearContrato` en contratos.js

**Files:**
- Modify: `worker/src/routes/contratos.js:97-155`

### Contexto

El `crearContrato` actual (línea 97) distingue `tipoContrato === 'estandar'` para generar el folio y usa un `fechaSesion` de nivel top del body. La nueva versión:
- Elimina la distinción de tipo (siempre `'estandar'`)
- Requiere `propsData` con al menos 1 elemento
- Genera folio desde `propsData[0].fechaSesion`
- Valida: máximo 20 props, formato de fecha, longitud de entregables

- [x] **Step 1: Abrir el archivo**

Abre `worker/src/routes/contratos.js`. El bloque `if (action === 'crearContrato')` empieza en línea 97 y termina en línea 155.

- [x] **Step 2: Reemplazar el bloque completo**

Reemplazar desde `if (action === 'crearContrato') {` hasta su `}` de cierre (línea 155) con:

```js
  if (action === 'crearContrato') {
    const body = await request.json();
    const { nombreCliente, correoCliente, telefonoCliente,
            tipoPaquete, paqueteBase, adicionales, extrasAcordados,
            precioTotal, anticipo, notasContrato, numPropiedades,
            propiedades: propsData } = body;

    if (!nombreCliente) return err('Nombre del cliente requerido');
    if (!propsData || !propsData.length) return err('Al menos una propiedad es requerida');
    if (propsData.length > 20) return err('Máximo 20 propiedades por contrato');

    const totalNum = parseFloat(precioTotal) || 0;
    if (totalNum <= 0) return err('El precio total debe ser mayor a $0');
    const anticNum = anticipo !== undefined && anticipo !== '' ? parseFloat(anticipo) || 0 : 0;

    // Validar fecha de propiedad 1
    const prop1 = propsData[0];
    const fechaRe = /^\d{4}-\d{2}-\d{2}$/;
    if (prop1.fechaSesion && !fechaRe.test(prop1.fechaSesion)) {
      return err('Formato de fecha inválido en propiedad 1 (esperado YYYY-MM-DD)');
    }
    // Validar longitud de entregables
    for (const p of propsData) {
      if (p.entregables && p.entregables.length > 2000) {
        return err('Entregables de propiedad ' + p.numPropiedad + ' exceden 2000 caracteres');
      }
    }

    const token = uuid();
    const paqueteBaseFinal = paqueteBase || prop1?.paquete || '';
    const tipoPaqueteFinal = tipoPaquete || prop1?.tipo || '';
    const paquete = await queryOne(db, 'SELECT precio FROM paquetes WHERE clave = ?', [paqueteBaseFinal]);
    const precioBase = paquete?.precio ?? totalNum;
    const saldoPendiente = Math.max(0, totalNum - anticNum);

    const adicionalesOfrecidos = (adicionales || []).filter(Boolean);
    const extrasObjs = (extrasAcordados || []).map(e =>
      e.clave ? { clave: e.clave, precio: e.precio } : { nombre: e.nombre, precio: e.precio }
    );
    const adicionalesJSON = JSON.stringify([...adicionalesOfrecidos, ...extrasObjs]);

    // Siempre generar folio desde propiedad 1
    const folio = prop1.fechaSesion ? generarFolio(prop1.fechaSesion) : null;

    await run(db,
      `INSERT INTO contratos (token, folio, nombre_cliente, correo_cliente, telefono_cliente,
       tipo_contrato, tipo_paquete, paquete_base, adicionales_json, precio_base, precio_total,
       anticipo, saldo_pendiente, estatus, fecha_creacion, num_propiedades, notas_contrato)
       VALUES (?, ?, ?, ?, ?, 'estandar', ?, ?, ?, ?, ?, ?, ?, 'Pendiente firma', ?, ?, ?)`,
      [token, folio, nombreCliente, correoCliente || '', telefonoCliente || '',
       tipoPaqueteFinal, paqueteBaseFinal,
       adicionalesJSON, precioBase, totalNum, anticNum, saldoPendiente,
       now(), numPropiedades || propsData.length, notasContrato || '']
    );

    for (const p of propsData) {
      await run(db,
        `INSERT INTO propiedades (contrato_token, num_propiedad, tipo, paquete, entregables,
         fecha_sesion, hora_sesion, direccion, link_maps, orientacion, sobre_la_propiedad,
         referencias, fachada_url, perimetro_url, logo_url, datos_especificos)
         VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
        [token, p.numPropiedad, p.tipo || tipoPaqueteFinal, p.paquete || paqueteBaseFinal,
         p.entregables || '', p.fechaSesion || '', p.horaSesion || '',
         p.direccion || '', p.linkMaps || '', p.orientacion || '',
         p.sobreLaPropiedad || '', p.referencias || '', p.fachadaUrl || '',
         p.perimetroUrl || '', p.logoUrl || '', JSON.stringify(p.datosEspecificos || {})]
      );
    }

    await crearTokenPortal(db, token, 72);

    const linkPortal = `https://contratos.inmueblesaudiovisuales.com/portal.html?token=${token}`;
    return ok({ ok: true, token, folio, url: linkPortal, linkPortal });
  }
```

- [x] **Step 3: Verificar que los imports siguen siendo suficientes**

Al inicio del archivo deben existir estos imports (ya existen, solo verificar):
```js
import { query, queryOne, run, batch, uuid, now } from '../db.js';
import { requireAdmin, ok, err } from '../auth.js';
import { callAdapter } from '../google.js';
import { generarFolio } from '../folios.js';
import { crearTokenPortal } from '../tokens.js';
```

- [x] **Step 4: Verificar sintaxis con wrangler**

```bash
cd "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/worker"
npx wrangler deploy --dry-run 2>&1 | tail -20
```

Resultado esperado: Sin errores de sintaxis. Puede mostrar advertencias de compatibilidad — ignorar.

---

## Task 2: Worker — portal.js: `firmaCliente` + `guardarConfiguracion` 410

**Files:**
- Modify: `worker/src/routes/portal.js:175` (una línea)
- Modify: `worker/src/routes/portal.js:266-330` (bloque guardarConfiguracion)

### Cambio 1: quitar guard de tipo en firmaCliente

- [x] **Step 1: Localizar línea 175 en portal.js**

La línea dice:
```js
    if (contrato.tipo_contrato !== 'particular' && adicionalesSeleccionados?.length) {
```

- [x] **Step 2: Reemplazar solo esa condición**

Cambiar a:
```js
    if (adicionalesSeleccionados?.length) {
```

### Cambio 2: deprecar guardarConfiguracion con 410

- [x] **Step 3: Localizar el bloque guardarConfiguracion**

En `portal.js` alrededor de la línea 266, hay un bloque grande que empieza con:
```js
  if (action === 'guardarConfiguracion') {
    const { token: tkStr, propiedades: propsData } = await request.json();
```

- [x] **Step 4: Reemplazar el bloque completo**

Reemplazar todo ese bloque (desde `if (action === 'guardarConfiguracion') {` hasta su `}` de cierre, incluyendo el `return ok({...})` al final) con:

```js
  if (action === 'guardarConfiguracion') {
    return err('Este endpoint ha sido deprecado en v5.0. Los contratos ahora se crean directamente con propiedades desde el admin.', 410);
  }
```

- [x] **Step 5: Verificar sintaxis**

```bash
cd "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/worker"
npx wrangler deploy --dry-run 2>&1 | tail -20
```

Sin errores de sintaxis.

---

## Task 3: Deploy del Worker + verificación

**Files:** ninguno (solo deploy)

- [x] **Step 1: Deploy completo**

```bash
cd "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/worker"
npx wrangler deploy
```

Esperado: `✓ Deployed` con URL `https://contratos.inmueblesaudiovisuales.com`.

- [x] **Step 2: Verificar `crearContrato` desde el admin**

Abrir `https://contratos.inmueblesaudiovisuales.com/admin.html` → "Nuevo contrato" → crear un contrato de prueba con propiedad 1 completa. Verificar que se genera folio con formato `IAV-AАММ.DD`.

- [x] **Step 3: Verificar 410 en guardarConfiguracion**

```bash
curl -s -X POST https://contratos.inmueblesaudiovisuales.com/api/guardarConfiguracion \
  -H "Content-Type: application/json" \
  -d '{"token":"test"}' -w "\nHTTP %{http_code}\n"
```

Esperado: `HTTP 410` con mensaje de deprecación.

- [x] **Step 4: Verificar rechazo cuando faltan propiedades**

```bash
curl -s -X POST https://contratos.inmueblesaudiovisuales.com/api/crearContrato \
  -H "Content-Type: application/json" \
  -d '{"nombreCliente":"Test","precioTotal":1000}' | python3 -m json.tool
```

Esperado: `{"ok":false,"error":"Al menos una propiedad es requerida"}` (el endpoint no requiere admin key para devolver error de validación — si devuelve 401, el worker está funcionando correctamente también).

- [x] **Step 5: Confirmar que firmaCliente no produce 500**

```bash
curl -s -X POST https://contratos.inmueblesaudiovisuales.com/api/firmaCliente \
  -H "Content-Type: application/json" \
  -d '{"token":"token-inexistente","correoCliente":"a@b.com","telefonoCliente":"8121234567","firmaBase64":"data:..."}' -w "\nHTTP %{http_code}\n"
```

Esperado: HTTP 404 (no 500).

---

## Task 4: Admin HTML — reemplazar sección HTML del formulario de nuevo contrato

**Files:**
- Modify: `frontend/admin.html:670-804`

### Contexto

Las líneas 670-804 contienen:
- `<div id="sec-nuevo">` con `.sub-tabs` (dos botones) y dos divs: `#form-estandar` y `#form-particular`
- El `#form-estandar` tiene: tipo de propiedad (radio), paquete base (select), adicionales, extras acordados, datos del cliente (nombre, correo, teléfono), fecha/hora, precio, anticipo
- El `#form-particular` tiene: nombre, correo, teléfono, notas, número de propiedades (select 1-5), container de prop cards, precio, anticipo

El nuevo HTML unificado:
- Sin sub-tabs
- Un solo form con datos del cliente primero, luego props dinámicas, luego adicionales, luego precio

- [x] **Step 1: Identificar el bloque exacto a reemplazar**

El bloque comienza en línea 670:
```html
    <!-- ── NUEVO CONTRATO ─────────────────────────────── -->
    <div id="sec-nuevo" class="seccion">
      <div class="form-card">
        <h2>Nuevo contrato</h2>
```

Y termina en línea 804 (después del cierre de `#form-particular`):
```html
      </div>
    </div>

    <!-- ── SESIONES ─────────────────────────────────── -->
```

- [x] **Step 2: Reemplazar el bloque con el nuevo HTML unificado**

Reemplazar desde la línea 670 hasta la línea 804 (incluyendo ambas) con:

```html
    <!-- ── NUEVO CONTRATO ─────────────────────────────── -->
    <div id="sec-nuevo" class="seccion">
      <div class="form-card">
        <h2>Nuevo contrato</h2>
        <p class="form-desc">Configura el contrato para tu cliente. Agrega una o más propiedades.</p>

        <!-- Datos del cliente -->
        <div class="campo">
          <label>Nombre del cliente</label>
          <input type="text" id="est-nombre" placeholder="Nombre completo">
        </div>
        <div class="fila-2">
          <div class="campo">
            <label>Correo <span style="font-weight:400;color:var(--ink-3)">(opcional)</span></label>
            <input type="email" id="est-correo" placeholder="correo@ejemplo.com">
          </div>
          <div class="campo">
            <label>Teléfono</label>
            <input type="text" id="est-telefono" placeholder="812 345 6789">
          </div>
        </div>

        <!-- Propiedades dinámicas -->
        <div id="props-container"></div>
        <button type="button" class="btn-secundario btn-sm" onclick="agregarPropiedad()" style="margin-bottom:16px">+ Agregar propiedad</button>

        <!-- Adicionales a ofrecer en el portal -->
        <div class="campo" id="campo-adicionales">
          <label>Opciones adicionales a ofrecer al cliente</label>
          <p style="font-size:11px;color:var(--ink-3);margin-bottom:8px;line-height:1.5">Las opciones marcadas aparecerán como toggles en el portal del cliente. El precio final se recalcula cuando el cliente las seleccione.</p>
          <div class="add-lista" id="lista-adicionales"></div>
        </div>

        <!-- Servicios extra ya acordados -->
        <div class="campo" style="background:var(--page);border:1px solid var(--border);border-radius:8px;padding:14px">
          <label style="margin-bottom:6px">Servicios extra ya acordados <span style="font-weight:400;color:var(--ink-3)">(opcional)</span></label>
          <p style="font-size:11px;color:var(--ink-3);margin-bottom:10px;line-height:1.5">Estos servicios están ya confirmados con el cliente y se suman al precio total desde el inicio. Aparecen en el PDF y en el portal bajo "Servicios adicionales".</p>
          <div id="campo-extras-catalogo" style="margin-bottom:8px"></div>
          <div id="lista-extras-acordados" style="display:flex;flex-direction:column;gap:6px;margin-bottom:8px"></div>
          <button type="button" class="btn-secundario btn-sm" onclick="agregarExtraLibre()" style="font-size:12px">+ Agregar servicio personalizado</button>
        </div>

        <hr style="border:none;border-top:1px solid var(--border);margin:16px 0">

        <!-- Precio y anticipo -->
        <div class="fila-2">
          <div class="campo">
            <label>Precio total <span style="font-weight:400;text-transform:none;letter-spacing:0">(editable)</span></label>
            <input type="number" id="est-precio-total" placeholder="0" oninput="precioManual=true">
          </div>
          <div class="campo">
            <label>Anticipo (50% por defecto)</label>
            <input type="number" id="est-anticipo" placeholder="0" oninput="anticipoManual=true">
          </div>
        </div>

        <div class="form-msg" id="msg-crear"></div>
        <button class="btn-primario gold" style="width:100%;margin-top:6px" id="btn-crear" onclick="crearContrato()">
          Crear contrato y generar link
        </button>
        <div id="resultado-crear"></div>
      </div>
    </div>
```

- [x] **Step 3: Verificar que el CSS `.par-tipo-btn` y `.activo-par` existen en el archivo**

```bash
grep -n "par-tipo-btn\|activo-par" "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/frontend/admin.html" | head -10
```

Esperado: al menos 2 líneas — una en el bloque `<style>` y las que quedan del código. Si no están en el CSS, agregar antes del cierre `</style>`:
```css
.par-tipo-btn {
  flex: 1;
  padding: 8px 12px;
  border: 1px solid var(--border);
  border-radius: 6px;
  background: var(--page);
  color: var(--ink-2);
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.15s;
}
.par-tipo-btn:hover { border-color: var(--gold); color: var(--ink-1); }
.par-tipo-btn.activo-par { background: var(--gold); color: white; border-color: var(--gold); }
```

---

## Task 5: Admin HTML — variables globales + nuevas funciones JS para prop cards

**Files:**
- Modify: `frontend/admin.html` (sección de variables globales ~línea 992 y funciones ~línea 2931)

> **Nota de secuencia:** Tasks 5 y 6 deben ejecutarse en orden antes del deploy (Task 7). `setTipoProp` (Task 5) llama `actualizarPaquetesAdicionales()` que se define en Task 6. No hacer deploy después de Task 5 sola.

### Contexto

El archivo tiene `var tiposPropPar = {};` en línea 992. Se reemplaza por `tiposProp` y se agrega `numProps`. Las funciones de prop cards del formulario particular (`renderPropCardsPar`, `setTipoPar`, `actualizarSelectPaquetePar`, `onPaqueteChangePar`) se MANTIENEN por ahora (se eliminan en Task 7). Se agregan las nuevas funciones JUNTO a ellas.

- [x] **Step 1: Actualizar la declaración de variables globales**

En línea 992, reemplazar:
```js
var tiposPropPar   = {};
```
con:
```js
var tiposPropPar   = {};  // legacy — se elimina en cleanup
var tiposProp      = {};  // formulario unificado: { 1: 'Residencial', 2: 'Terreno', ... }
var numProps       = 1;   // número de propiedades en el formulario unificado
```

- [x] **Step 2: Agregar función `leerEstadoProps`**

Agregar ANTES de la función `renderPropCardsPar` (línea 2933), insertar:

```js
// ── FORMULARIO UNIFICADO — PROP CARDS ────────────────────────────────────────

function leerEstadoProps() {
  var estado = {};
  for (var i = 1; i <= numProps; i++) {
    estado[i] = {
      nombreServicio: (document.getElementById('prop-nombre-servicio-' + i) || {value:''}).value,
      paquete:        (document.getElementById('prop-paquete-' + i)        || {value:''}).value,
      entregables:    (document.getElementById('prop-entregables-' + i)    || {value:''}).value,
      fecha:          (document.getElementById('prop-fecha-' + i)          || {value:''}).value,
      hora:           (document.getElementById('prop-hora-' + i)           || {value:'10:00'}).value,
    };
  }
  return estado;
}

function restaurarEstadoProps(estado) {
  for (var i = 1; i <= numProps; i++) {
    var s = estado[i];
    if (!s) continue;
    var nomEl  = document.getElementById('prop-nombre-servicio-' + i);
    var entEl  = document.getElementById('prop-entregables-' + i);
    var fechEl = document.getElementById('prop-fecha-' + i);
    var horaEl = document.getElementById('prop-hora-' + i);
    var paqEl  = document.getElementById('prop-paquete-' + i);
    if (nomEl  && s.nombreServicio) nomEl.value  = s.nombreServicio;
    if (entEl  && s.entregables)    entEl.value  = s.entregables;
    if (fechEl && s.fecha)          fechEl.value = s.fecha;
    if (horaEl && s.hora)           horaEl.value = s.hora;
    if (paqEl  && s.paquete) {
      for (var j = 0; j < paqEl.options.length; j++) {
        if (paqEl.options[j].value === s.paquete) { paqEl.selectedIndex = j; break; }
      }
    }
  }
}

function renderPropCard(i, tipo) {
  return '<div class="prop-card-par" style="background:var(--page);border:1px solid var(--border);border-radius:var(--r);padding:16px;margin-bottom:12px">' +
    '<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">' +
      '<div style="font-size:12px;font-weight:700;color:var(--ink-2)">PROPIEDAD ' + i + '</div>' +
      (numProps > 1 ? '<button type="button" class="btn-secundario btn-sm" onclick="quitarPropiedad(' + i + ')" style="font-size:11px;padding:2px 8px">Quitar</button>' : '') +
    '</div>' +
    '<div class="campo">' +
      '<label>Tipo de propiedad</label>' +
      '<div style="display:flex;gap:8px">' +
        '<button type="button" class="par-tipo-btn' + (tipo !== 'Terreno' ? ' activo-par' : '') + '" data-prop="' + i + '" data-tipo="Residencial" onclick="setTipoProp(' + i + ',\'Residencial\')">Residencial</button>' +
        '<button type="button" class="par-tipo-btn' + (tipo === 'Terreno' ? ' activo-par' : '') + '" data-prop="' + i + '" data-tipo="Terreno" onclick="setTipoProp(' + i + ',\'Terreno\')">Terreno</button>' +
      '</div>' +
    '</div>' +
    '<div class="campo">' +
      '<label>Nombre del servicio</label>' +
      '<input type="text" id="prop-nombre-servicio-' + i + '" placeholder="Ej: Producción audiovisual residencial, Tour virtual aéreo...">' +
    '</div>' +
    '<div class="campo">' +
      '<label>Paquete base <span style="font-weight:400;color:var(--ink-3)">(opcional — llena entregables automáticamente)</span></label>' +
      '<select id="prop-paquete-' + i + '" onchange="onPaqueteChangeProp(' + i + ')"><option value="">— Sin paquete —</option></select>' +
    '</div>' +
    '<div class="campo">' +
      '<label>Entregables</label>' +
      '<textarea id="prop-entregables-' + i + '" maxlength="2000" placeholder="Se llena automáticamente al elegir paquete. Puedes editarlo." style="min-height:70px"></textarea>' +
    '</div>' +
    '<div class="fila-2">' +
      '<div class="campo"><label>Fecha de sesión</label><input type="date" id="prop-fecha-' + i + '"></div>' +
      '<div class="campo"><label>Hora</label><input type="time" id="prop-hora-' + i + '" value="10:00"></div>' +
    '</div>' +
  '</div>';
}

function renderTodasLasProps() {
  var estado = leerEstadoProps();
  var container = document.getElementById('props-container');
  if (!container) return;
  var html = '';
  for (var i = 1; i <= numProps; i++) {
    if (!tiposProp[i]) tiposProp[i] = 'Residencial';
    html += renderPropCard(i, tiposProp[i]);
  }
  container.innerHTML = html;
  for (var j = 1; j <= numProps; j++) actualizarSelectPaqueteProp(j);
  restaurarEstadoProps(estado);
}

function agregarPropiedad() {
  numProps++;
  tiposProp[numProps] = 'Residencial';
  renderTodasLasProps();
}

function quitarPropiedad(num) {
  if (numProps <= 1) return;
  var estado = leerEstadoProps();
  // Renumerar tipos y estado excluyendo la propiedad removida
  var nuevosTipos = {};
  var nuevoEstado = {};
  var j = 1;
  for (var i = 1; i <= numProps; i++) {
    if (i === num) continue;
    nuevosTipos[j] = tiposProp[i] || 'Residencial';
    nuevoEstado[j] = estado[i] || {};
    j++;
  }
  tiposProp = nuevosTipos;
  numProps--;
  // Renderizar con numProps actualizado, luego restaurar estado renumerado
  var container = document.getElementById('props-container');
  if (!container) return;
  var html = '';
  for (var k = 1; k <= numProps; k++) {
    html += renderPropCard(k, tiposProp[k]);
  }
  container.innerHTML = html;
  for (var m = 1; m <= numProps; m++) actualizarSelectPaqueteProp(m);
  restaurarEstadoProps(nuevoEstado);
}

function setTipoProp(num, tipo) {
  tiposProp[num] = tipo;
  // Actualizar solo clases de botones — sin re-render completo (preserva datos escritos)
  var btns = document.querySelectorAll('.par-tipo-btn[data-prop="' + num + '"]');
  btns.forEach(function(b) {
    b.classList.toggle('activo-par', b.dataset.tipo === tipo);
  });
  actualizarSelectPaqueteProp(num);
  actualizarPaquetesAdicionales(); // actualizar adicionales según tipo de prop 1
}

function actualizarSelectPaqueteProp(num) {
  var select = document.getElementById('prop-paquete-' + num);
  if (!select) return;
  var tipo = tiposProp[num] || 'Residencial';
  var valorActual = select.value; // preservar selección si existe en el nuevo tipo
  var disponibles = paquetes.filter(function(p) {
    if (p.EsAdicional) return false;
    return p.Tipo === tipo || p.Tipo === 'Ambos';
  });
  select.innerHTML = '<option value="">— Sin paquete —</option>';
  disponibles.forEach(function(p) {
    var opt = document.createElement('option');
    opt.value = p.Clave;
    opt.dataset.precio      = p.Precio    || 0;
    opt.dataset.entregables = p.Entregables || '';
    opt.dataset.nombre      = p.Nombre    || p.Clave;
    opt.textContent = (p.Nombre || p.Clave) + (p.Precio ? ' — ' + fmxn(p.Precio) : '');
    select.appendChild(opt);
  });
  // Restaurar selección si el paquete sigue disponible en el nuevo tipo
  if (valorActual) {
    for (var i = 0; i < select.options.length; i++) {
      if (select.options[i].value === valorActual) { select.selectedIndex = i; break; }
    }
  }
}

function onPaqueteChangeProp(num) {
  var select   = document.getElementById('prop-paquete-' + num);
  var textarea = document.getElementById('prop-entregables-' + num);
  if (!select || !textarea) return;
  var val = select.value;
  if (!val) return; // no limpiar si ya había texto libre
  var opt = select.options[select.selectedIndex];
  textarea.value = opt.dataset.entregables || '';
  actualizarPrecio(); // recalcular precio al cambiar paquete
}
```

- [x] **Step 3: Verificar que no hay errores de sintaxis**

```bash
grep -c "function " "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/frontend/admin.html"
```

Debería retornar un número mayor a 50 (solo cuenta funciones, no valida sintaxis). Para validación real, abrir el archivo en el navegador con la consola abierta y verificar que no hay `SyntaxError`.

---

## Task 6: Admin HTML — reemplazar funciones JS existentes

**Files:**
- Modify: `frontend/admin.html` (múltiples funciones JS)

### 6.1 Actualizar `cargarPaquetes` (línea 1411)

- [x] **Step 1: Reemplazar `cargarPaquetes`**

Encontrar la función `cargarPaquetes` (línea 1411) y reemplazar:
```js
async function cargarPaquetes() {
  try {
    const data = await apiGet({ action: 'listarPaquetes' });
    if (data.ok) {
      paquetes = data.paquetes;
      actualizarPaquetes();
      renderAdicionalesPar();
    }
  } catch (e) { console.error('Error cargando paquetes:', e); }
}
```
con:
```js
async function cargarPaquetes() {
  try {
    const data = await apiGet({ action: 'listarPaquetes' });
    if (data.ok) {
      paquetes = data.paquetes;
      actualizarPaquetesAdicionales();
      renderTodasLasProps();
    }
  } catch (e) { console.error('Error cargando paquetes:', e); }
}
```

### 6.2 Reemplazar `actualizarPaquetes` con `actualizarPaquetesAdicionales`

- [x] **Step 2: Reemplazar la función completa (línea 1427)**

Encontrar la función `actualizarPaquetes` (línea 1427) y reemplazarla completa con:

```js
function actualizarPaquetesAdicionales() {
  var tipo = tiposProp[1] || 'Residencial'; // filtrar por tipo de prop 1
  var adds = paquetes.filter(function(p) { return p.EsAdicional && (p.Tipo === tipo || p.Tipo === 'Ambos'); });

  var lista    = document.getElementById('lista-adicionales');
  var campoAdd = document.getElementById('campo-adicionales');
  if (!lista || !campoAdd) return;

  if (!adds.length) {
    campoAdd.style.display = 'none';
    lista.innerHTML = '';
  } else {
    campoAdd.style.display = 'block';
    lista.innerHTML = adds.map(function(p) {
      return '<label class="add-item">' +
        '<input type="checkbox" value="' + esc(p.Clave) + '" data-precio="' + p.Precio + '">' +
        '<span class="add-item-nombre">' + esc(p.Nombre) + '</span>' +
        '<span class="add-item-precio">+' + fmxn(p.Precio) + '</span>' +
        '</label>';
    }).join('');
  }

  var contCatalogo = document.getElementById('campo-extras-catalogo');
  if (contCatalogo) {
    if (!adds.length) {
      contCatalogo.innerHTML = '';
    } else {
      contCatalogo.innerHTML = '<p style="font-size:11px;color:var(--ink-3);margin-bottom:6px">Del catálogo:</p>' +
        adds.map(function(p) {
          return '<label class="add-item" style="margin-bottom:4px">' +
            '<input type="checkbox" class="extra-acordado-cat-cb" value="' + esc(p.Clave) + '" data-precio="' + p.Precio + '" onchange="actualizarPrecio()">' +
            '<span class="add-item-nombre">' + esc(p.Nombre) + '</span>' +
            '<span class="add-item-precio">+' + fmxn(p.Precio) + '</span>' +
            '</label>';
        }).join('');
    }
  }

  actualizarPrecio();
}
```

### 6.3 Reemplazar `actualizarPrecio`

- [x] **Step 3: Reemplazar la función `actualizarPrecio` (línea 1473)**

Encontrar la función `actualizarPrecio` y reemplazarla completa:

```js
function actualizarPrecio() {
  if (precioManual) return;
  var baseTotal = 0;
  for (var i = 1; i <= numProps; i++) {
    var sel = document.getElementById('prop-paquete-' + i);
    if (sel && sel.value) {
      var opt = sel.options[sel.selectedIndex];
      baseTotal += parseFloat((opt && opt.dataset && opt.dataset.precio) || 0) || 0;
    }
  }
  var sumaExtras = 0;
  document.querySelectorAll('.extra-acordado-cat-cb:checked').forEach(function(cb) {
    sumaExtras += parseFloat(cb.dataset.precio) || 0;
  });
  document.querySelectorAll('.extra-libre-precio').forEach(function(inp) {
    sumaExtras += parseFloat(inp.value) || 0;
  });
  document.getElementById('est-precio-total').value = baseTotal + sumaExtras;
  actualizarAnticipo('est');
}
```

### 6.4 Reemplazar `crearContrato`

- [x] **Step 4: Reemplazar la función `crearContrato(tipo)` (línea 2458)**

Encontrar `async function crearContrato(tipo) {` y reemplazar la función completa con:

```js
async function crearContrato() {
  var msgId = 'msg-crear';
  var resId = 'resultado-crear';
  var btn   = document.getElementById('btn-crear');

  setMsg(msgId, '', '');
  document.getElementById(resId).innerHTML = '';

  var nombre   = document.getElementById('est-nombre').value.trim();
  var correo   = document.getElementById('est-correo').value.trim();
  var telefono = document.getElementById('est-telefono').value.trim();
  var ptotal   = parseFloat(document.getElementById('est-precio-total').value) || 0;
  var anticipo = parseFloat(document.getElementById('est-anticipo').value)     || 0;

  if (!nombre) { setMsg(msgId, 'El nombre del cliente es obligatorio.', 'error'); return; }
  if (!ptotal) { setMsg(msgId, 'El precio total es obligatorio.',       'error'); return; }

  var adds = Array.from(document.querySelectorAll('#lista-adicionales input[type="checkbox"]:checked')).map(function(c) { return c.value; });

  var extrasAcordados = [];
  document.querySelectorAll('.extra-acordado-cat-cb:checked').forEach(function(cb) {
    extrasAcordados.push({ clave: cb.value, precio: parseFloat(cb.dataset.precio) || 0 });
  });
  document.querySelectorAll('#lista-extras-acordados > div').forEach(function(fila) {
    var nombreSvc = (fila.querySelector('.extra-libre-nombre') || {value:''}).value.trim();
    var precioSvc = parseFloat((fila.querySelector('.extra-libre-precio') || {value:''}).value) || 0;
    if (nombreSvc && precioSvc) extrasAcordados.push({ nombre: nombreSvc, precio: precioSvc });
  });

  var propiedades  = [];
  var paqueteBase  = null;
  var tipoPaquete  = 'Residencial';

  for (var i = 1; i <= numProps; i++) {
    var tipoProp     = tiposProp[i] || 'Residencial';
    var nombreSvc    = (document.getElementById('prop-nombre-servicio-' + i) || {value:''}).value.trim();
    var paqSel       = document.getElementById('prop-paquete-' + i);
    var paqClave     = paqSel ? paqSel.value : '';
    var entregables  = (document.getElementById('prop-entregables-' + i)    || {value:''}).value.trim();
    var fechaProp    = (document.getElementById('prop-fecha-' + i)           || {value:''}).value;
    var horaProp     = (document.getElementById('prop-hora-' + i)            || {value:'10:00'}).value;

    if (!nombreSvc && !paqClave) {
      setMsg(msgId, 'Propiedad ' + i + ': necesitas un nombre de servicio o elegir un paquete.', 'error');
      return;
    }
    if (!fechaProp) {
      setMsg(msgId, 'Propiedad ' + i + ': falta fecha de sesión.', 'error');
      return;
    }

    var paqueteVal = paqClave || nombreSvc;
    if (i === 1) { paqueteBase = paqueteVal; tipoPaquete = tipoProp; }

    propiedades.push({
      numPropiedad: i,
      tipo:         tipoProp,
      paquete:      paqueteVal,
      entregables:  entregables,
      fechaSesion:  fechaProp,
      horaSesion:   horaProp,
    });
  }

  var body = {
    action:          'crearContrato',
    nombreCliente:   nombre,
    correoCliente:   correo,
    telefonoCliente: telefono,
    tipoPaquete:     tipoPaquete,
    paqueteBase:     paqueteBase,
    adicionales:     adds,
    extrasAcordados: extrasAcordados,
    precioTotal:     ptotal,
    anticipo:        anticipo,
    numPropiedades:  numProps,
    propiedades:     propiedades,
  };

  btn.disabled    = true;
  btn.textContent = 'Creando...';

  try {
    var data = await apiPost(body);
    if (!data.ok) throw new Error(data.error || 'Error desconocido');

    var waTxt    = 'Hola, aquí está tu link para revisar y firmar tu contrato con Inmuebles Audiovisuales: ' + data.url;
    var waTelN   = normalizarTelWA(telefono);
    var waUrl    = 'https://wa.me/' + waTelN + '?text=' + encodeURIComponent(waTxt);

    document.getElementById(resId).innerHTML =
      '<div class="resultado-box">' +
        '<div class="resultado-titulo">Link del portal para el cliente</div>' +
        '<div class="resultado-url">' + esc(data.url) + '</div>' +
        '<div class="resultado-acciones">' +
          '<button class="btn-copiar" data-url="' + esc(data.url) + '" onclick="copiarDesdeBtn(this)">' +
            '<i class="ti ti-copy"></i> Copiar link' +
          '</button>' +
          '<a class="btn-wa" href="' + esc(waUrl) + '" target="_blank" rel="noopener">' +
            '<i class="ti ti-brand-whatsapp"></i> WhatsApp' +
          '</a>' +
        '</div>' +
      '</div>';

    limpiarFormCrear();
    cargarContratos();
    _clientesCache = [];
    cargarClientesParaAutocomplete();
  } catch (e) {
    setMsg(msgId, 'Error: ' + e.message, 'error');
  } finally {
    btn.disabled    = false;
    btn.textContent = 'Crear contrato y generar link';
  }
}
```

### 6.5 Reemplazar `limpiarFormCrear`

- [x] **Step 5: Reemplazar la función `limpiarFormCrear(tipo)` (línea 2617)**

Encontrar `function limpiarFormCrear(tipo) {` y reemplazar la función completa:

```js
function limpiarFormCrear() {
  ['est-nombre','est-correo','est-telefono'].forEach(function(id) {
    var el = document.getElementById(id);
    if (el) el.value = '';
  });
  document.getElementById('est-precio-total').value = '';
  document.getElementById('est-anticipo').value     = '';

  var listaLibres = document.getElementById('lista-extras-acordados');
  if (listaLibres) listaLibres.innerHTML = '';
  document.querySelectorAll('.extra-acordado-cat-cb').forEach(function(cb) { cb.checked = false; });

  precioManual   = false;
  anticipoManual = false;
  numProps       = 1;
  tiposProp      = { 1: 'Residencial' };
  renderTodasLasProps();
  actualizarPaquetesAdicionales();
  cerrarDropdownAC();

  var logoHint = document.getElementById('ac-logo-hint-est');
  if (logoHint) logoHint.remove();
}
```

### 6.6 Actualizar `duplicarContrato`

- [x] **Step 6: Reemplazar la función `duplicarContrato` (línea 1322)**

Encontrar `function duplicarContrato() {` y reemplazar la función completa:

```js
function duplicarContrato() {
  var ctx = window._contratoActivo;
  if (!ctx) return;
  var c           = ctx.c;
  var adicionales = ctx.adicionales || [];
  var propiedades = ctx.propiedades || [];
  cerrarPanel();
  irANuevo();
  requestAnimationFrame(function() {
    // Datos del cliente
    document.getElementById('est-nombre').value   = c.NombreCliente   || '';
    document.getElementById('est-correo').value   = c.CorreoCliente   || '';
    document.getElementById('est-telefono').value = c.TelefonoCliente || '';

    // Configurar propiedades desde el contrato original
    numProps  = Math.max(1, propiedades.length);
    tiposProp = {};
    propiedades.forEach(function(p, i) { tiposProp[i + 1] = p.Tipo || 'Residencial'; });
    if (!propiedades.length) tiposProp[1] = 'Residencial';
    renderTodasLasProps();

    // Poblar cada card con datos del contrato original
    propiedades.forEach(function(p, i) {
      var n      = i + 1;
      var nomEl  = document.getElementById('prop-nombre-servicio-' + n);
      var paqEl  = document.getElementById('prop-paquete-'         + n);
      var entEl  = document.getElementById('prop-entregables-'     + n);
      var fechEl = document.getElementById('prop-fecha-'           + n);
      var horaEl = document.getElementById('prop-hora-'            + n);

      if (nomEl)  nomEl.value  = p.Paquete     || '';
      if (entEl)  entEl.value  = p.Entregables || '';
      if (fechEl) fechEl.value = p.FechaSesion || '';
      if (horaEl) horaEl.value = p.HoraSesion  || '10:00';
      // Intentar seleccionar clave de catálogo si existe
      if (paqEl && p.Paquete) {
        for (var j = 0; j < paqEl.options.length; j++) {
          if (paqEl.options[j].value === p.Paquete) { paqEl.selectedIndex = j; break; }
        }
      }
    });

    // Marcar adicionales ofrecidos (strings)
    adicionales.filter(function(item) { return typeof item === 'string'; }).forEach(function(clave) {
      var cb = document.querySelector('#lista-adicionales input[value="' + CSS.escape(clave) + '"]');
      if (cb) cb.checked = true;
    });

    precioManual   = true;
    anticipoManual = true;
    document.getElementById('est-precio-total').value = parseFloat(c.PrecioTotal) || '';
    document.getElementById('est-anticipo').value     = parseFloat(c.Anticipo)    || '';
  });
}
```

### 6.7 Eliminar funciones y variables obsoletas

- [x] **Step 7: Eliminar `mostrarSubTab`**

Encontrar la función `mostrarSubTab(tipo, btn)` (línea 1175) y eliminarla completa.

- [x] **Step 8: Eliminar `renderPropCardsPar`, `setTipoPar`, `actualizarSelectPaquetePar`, `onPaqueteChangePar`**

Encontrar el bloque comentado `// ── CONTRATOS PARTICULARES — CARDS POR PROPIEDAD` (línea 2931) y eliminar las cuatro funciones que contiene: `renderPropCardsPar`, `setTipoPar`, `actualizarSelectPaquetePar`, `onPaqueteChangePar`.

- [x] **Step 9: Eliminar `renderAdicionalesPar`**

Encontrar la función `renderAdicionalesPar()` (línea 1422) y eliminarla completa.

- [x] **Step 10: Eliminar la declaración `var tiposPropPar = {};`**

En la línea de globals (~992), encontrar:
```js
var tiposPropPar   = {};  // legacy — se elimina en cleanup
```
Y eliminarla.

- [x] **Step 11: Verificar que no quedan referencias a funciones eliminadas**

```bash
grep -n "tiposPropPar\|mostrarSubTab\|renderPropCardsPar\|setTipoPar\b\|actualizarSelectPaquetePar\|onPaqueteChangePar\|renderAdicionalesPar\|actualizarPaquetes()\|form-estandar\|form-particular\|sub-tab-btn\|par-num-props\|par-notas\|btn-crear-estandar\|btn-crear-particular\|msg-estandar\|msg-particular\|resultado-estandar\|resultado-particular" \
  "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/frontend/admin.html"
```

Esperado: 0 líneas. Si aparece alguna, eliminarla.

---

## Task 7: Deploy Admin + verificación manual

**Files:** ninguno (solo deploy y pruebas)

- [x] **Step 1: Deploy completo**

```bash
cd "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/worker"
npx wrangler deploy
```

- [x] **Step 2: Abrir admin en el navegador**

Ir a `https://contratos.inmueblesaudiovisuales.com/admin.html`. Abrir la consola del navegador (F12 → Console) antes de hacer login.

- [x] **Step 3: Verificar que no hay errores de JS en consola**

Esperado: sin `ReferenceError`, `TypeError`, ni `SyntaxError` en la carga inicial.

- [x] **Step 4: Verificar creación de contrato con 1 propiedad y paquete de catálogo**

1. Ir a "Nuevo contrato"
2. Escribir nombre del cliente
3. En "PROPIEDAD 1": tipo Residencial, elegir paquete del catálogo (ej. RES-COMBO)
4. Verificar que el precio total se llena automáticamente
5. Escribir fecha de sesión
6. Clic en "Crear contrato"
7. Esperado: aparece link del portal con folio correcto

- [x] **Step 5: Verificar creación con servicio libre (sin paquete de catálogo)**

1. Nuevo contrato
2. En "PROPIEDAD 1": tipo Terreno, dejar el select en "— Sin paquete —"
3. Escribir nombre del servicio: "Levantamiento aéreo de terreno"
4. Escribir entregables manualmente
5. Fecha de sesión, precio manual ($3000)
6. Clic en "Crear contrato"
7. Esperado: contrato creado, folio generado

- [x] **Step 6: Verificar contrato con 2 propiedades**

1. Nuevo contrato
2. Propiedad 1: Residencial, RES-COMBO, fecha
3. Clic "+ Agregar propiedad"
4. Propiedad 2: Terreno, servicio libre, fecha diferente
5. Verificar que cambiar tipo en Propiedad 2 NO borra los datos de Propiedad 1
6. Crear contrato
7. Esperado: contrato creado con 2 propiedades

- [x] **Step 7: Verificar `duplicarContrato`**

1. Abrir un contrato existente en el panel lateral
2. Clic en "Duplicar"
3. Verificar que el formulario se llena con los datos del contrato original

---

## Task 8: Portal HTML — eliminar `esParticular`

**Files:**
- Modify: `frontend/portal.html` (líneas 557-697, 980, 1282-1331)

### Contexto

El portal tiene `var esParticular = d.tipoContrato === 'particular'` en dos lugares (líneas 561 y 980) que controlan 7 condicionales. Los cambios aplican guards defensivos sobre los datos (en lugar de sobre el tipo) para que contratos `particular` existentes no rompan la UI.

### 8.1 Cambios en `renderEtapa1` (líneas 557-697)

- [x] **Step 1: Eliminar declaración de `esParticular` en `renderEtapa1` (línea 561)**

Encontrar:
```js
  var esParticular = d.tipoContrato === 'particular';
```
Eliminarla.

- [x] **Step 2: Cambiar condición para mostrar paquete/entregables de prop 0 (línea 578)**

Encontrar:
```js
  if (!esParticular && props.length > 0) {
```
Cambiar a:
```js
  if (props.length === 1) {
```
(Esta sección muestra el resumen inline — solo para contratos de 1 propiedad.)

- [x] **Step 3: Cambiar condición para mostrar extras acordados (línea 601)**

Encontrar:
```js
  if (extrasAcordados.length > 0 && !esParticular) {
```
Cambiar a:
```js
  if (extrasAcordados.length > 0) {
```

- [x] **Step 4: Cambiar condición para mostrar "Precio base" (línea 623)**

Encontrar:
```js
  if (precioBase > 0 && !esParticular && addons.length > 0) {
```
Cambiar a:
```js
  if (precioBase > 0 && addons.length > 0) {
```

- [x] **Step 5: Cambiar condición de notas del contrato (línea 629)**

Encontrar:
```js
  if (esParticular && d.notasContrato) {
```
Cambiar a:
```js
  if (d.notasContrato) {
```

- [x] **Step 6: Cambiar condición de resúmenes de propiedades múltiples (línea 634)**

Encontrar:
```js
  if (esParticular && props.length > 0) {
    props.forEach(function(p, i) {
      html += '<div class="prop-resumen">...';
      if (p.entregables) {
        var items = p.entregables.split('·').map(...)
```
Cambiar la condición y corregir el split de entregables:

```js
  if (props.length > 1) {
    props.forEach(function(p, i) {
      html += '<div class="prop-resumen"><div class="prop-resumen-header"><div class="prop-badge">' + (i + 1) + '</div><span class="prop-resumen-nombre">' + esc(p.paquete || 'Propiedad ' + (i + 1)) + '</span></div>';
      if (p.fechaSesion) html += '<p class="prop-resumen-fecha">' + esc(formatFechaHora(p.fechaSesion, p.horaSesion)) + '</p>';
      if (p.entregables) {
        var items = p.entregables.replace(/\\n/g, '\n').split(/[·|\n]/).map(function(e) { return e.trim(); }).filter(Boolean);
        if (items.length) html += '<ul class="prop-entregables">' + items.map(function(e) { return '<li>' + esc(e) + '</li>'; }).join('') + '</ul>';
      }
      html += '</div>';
    });
  }
```

- [x] **Step 7: Cambiar condición para add-ons (línea 648)**

Encontrar:
```js
  if (addons.length > 0 && !esParticular) {
```
Cambiar a:
```js
  if (addons.length > 0) {
```

- [x] **Step 8: Cambiar header del card de propiedad en el formulario (línea 692)**

Encontrar el bloque:
```js
      if (!esParticular) {
        html += '<div class="prop-badge">' + n + '</div><div>...paquete...fecha...</div>';
      } else {
        html += '<div class="prop-badge">' + n + '</div><div><p ...>Propiedad ' + n + '</p></div>';
      }
```
Reemplazar con (siempre usar el formato rico con paquete y fecha):
```js
      html += '<div class="prop-badge">' + n + '</div><div>';
      html += '<p style="font-size:13px;font-weight:700">' + esc(p.paquete || 'Propiedad ' + n) + '</p>';
      if (p.fechaSesion) html += '<p style="font-size:11px;color:var(--concrete);margin-top:2px">' + esc(formatFechaHora(p.fechaSesion, p.horaSesion)) + '</p>';
      html += '</div>';
```

### 8.2 Cambios en `validarEtapa1` (línea 980)

- [x] **Step 9: Eliminar `esParticular` en `validarEtapa1`**

Encontrar (línea 980):
```js
  var esParticular = portalData && portalData.tipoContrato === 'particular';
```
Eliminarla. No se usa en el loop de validación que sigue — la variable existía por si se necesitaba, pero el loop ya valida todas las props.

### 8.3 Cambios en `renderRevision` (línea 1282)

- [x] **Step 10: Eliminar `esP` y unificar renderizado de props**

Encontrar (línea 1285):
```js
  var esP   = d.tipoContrato === 'particular';
```
Eliminarla.

Encontrar el bloque condicional que empieza en línea 1309:
```js
  if (esP) {
    props.forEach(function(p, i) {
      ...
    });
  } else {
    formData.propsPayload.forEach(function(p, i) {
      ...
    });
  }
```
Reemplazar el bloque completo (ambas ramas) con el bloque unificado (la rama `esP` es más rica — incluye entregables, dirección, orientación):

```js
  props.forEach(function(p, i) {
    var fp    = formData.propsPayload[i] || {};
    var label = props.length > 1 ? 'Propiedad ' + (i + 1) : 'Paquete';
    if (p.paquete) html += '<div class="resumen-fila"><span class="resumen-label">' + label + '</span><span class="resumen-valor">' + esc(p.paquete) + '</span></div>';
    if (p.fechaSesion) html += '<div class="resumen-fila"><span class="resumen-label">Fecha de sesión</span><span class="resumen-valor">' + esc(formatFechaHora(p.fechaSesion, p.horaSesion)) + '</span></div>';
    if (p.entregables) {
      var items = p.entregables.replace(/\\n/g, '\n').split(/[·|\n]/).map(function(e) { return e.trim(); }).filter(Boolean);
      if (items.length) html += '<div class="resumen-fila"><span class="resumen-label">Entregables</span><span class="resumen-valor" style="text-align:right">' + items.map(function(e) { return esc(e.trim()); }).join('<br>') + '</span></div>';
    }
    if (fp.direccion) html += '<div class="resumen-fila"><span class="resumen-label">Dirección</span><span class="resumen-valor">' + esc(fp.direccion) + '</span></div>';
    if (fp.orientacion) html += '<div class="resumen-fila"><span class="resumen-label">Orientación del video</span><span class="resumen-valor">' + esc(fp.orientacion.split(' — ')[0] || fp.orientacion) + '</span></div>';
  });
```

- [x] **Step 11: Verificar que no quedan referencias a `esParticular` o `esP`**

```bash
grep -n "esParticular\|var esP " "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/frontend/portal.html"
```

Esperado: 0 líneas.

---

## Task 9: Deploy Portal + verificación final completa

**Files:** ninguno (solo deploy y pruebas)

- [x] **Step 1: Deploy completo**

```bash
cd "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/worker"
npx wrangler deploy
```

- [x] **Step 2: Verificar portal con contrato nuevo (1 propiedad)**

1. Crear un contrato desde el admin (1 prop, paquete de catálogo)
2. Abrir el link del portal
3. Verificar: se muestra paquete, fecha, entregables en el resumen
4. Completar todos los campos
5. Ir a revisión — verificar que aparecen entregables, dirección, orientación
6. Firmar
7. Esperado: firma exitosa, estatus cambia a "Firmado" o "En produccion"

- [x] **Step 3: Verificar portal con contrato nuevo (2 propiedades)**

1. Crear contrato con 2 propiedades desde el admin
2. Abrir portal
3. Verificar: se muestran dos cards de resumen con sus datos respectivos
4. Completar formulario de ambas propiedades
5. Ir a revisión — ver ambas propiedades con sus datos
6. Firmar
7. Esperado: firma exitosa

- [x] **Step 4: Verificar portal con contrato particular existente (retrocompatibilidad)**

1. Buscar un contrato con `tipo_contrato = 'particular'` en la lista de admin
2. Abrir su portal
3. Verificar: no hay errores de JS en consola, UI se muestra sin labels vacíos ni errores
4. No es necesario firmar — solo verificar que carga correctamente

- [x] **Step 5: Verificar que add-ons funcionan en contrato con add-ons disponibles**

1. Crear contrato con `adicionales: ["ADD-COMOLLEGAR"]` (o el que exista)
2. Abrir portal
3. Verificar que aparece la sección de add-ons opcionales
4. Activar un add-on, verificar que el total se actualiza
5. Continuar al formulario y firmar
6. Verificar que el total en el backend incluye el add-on seleccionado

---

## Task 10: Limpieza final

**Files:**
- Modify: `frontend/admin.html` (CSS dead code)
- Verify: `adapter/AdapterScript4_v1.js`

- [x] **Step 1: Verificar que el adapter no tiene lógica condicional sobre `tipo_contrato`**

```bash
grep -n "tipo_contrato" "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/adapter/AdapterScript4_v1.js"
```

Revisar cada línea que aparezca. Si `tipo_contrato` solo aparece como parte de texto (ej. en asuntos de correo o descripciones), está bien. Si aparece en un `if (body.contrato.tipo_contrato === ...)`, ese bloque debe analizarse. No hay acción requerida si no hay lógica condicional.

- [x] **Step 2: Limpiar CSS obsoleto en admin.html**

Verificar si existen selectores CSS de elementos ya eliminados:
```bash
grep -n "\.sub-tab-btn\|#form-particular\|#form-estandar\|\.sub-tabs" \
  "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/frontend/admin.html"
```

Si aparecen en el bloque `<style>`, eliminar esas reglas CSS. Si no aparecen o aparecen solo en código eliminado, no hacer nada.

- [x] **Step 3: Deploy final**

```bash
cd "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/worker"
npx wrangler deploy
```

- [x] **Step 4: Verificar conteo final en D1**

```bash
cd "/Users/brunogutierrez/Documents/CLAUDE CODE/Inmuebles WEBSITE/02. contratos/06. VERSION 4.0/worker"
npx wrangler d1 execute iav-contratos --remote --command "SELECT tipo_contrato, COUNT(*) as total FROM contratos GROUP BY tipo_contrato;"
```

Esperado: Los contratos nuevos son todos `'estandar'`. Los particulares existentes siguen ahí pero no se crean nuevos.

---

## Checklist de bugs cubiertos

Todos los problemas de `PLAN_UNIFICACION_V5.md §Advertencias` deben estar resueltos:

| Problema | Cubierto en |
|----------|------------|
| P1 — `setTipoProp` destruye datos escritos | Task 5, `setTipoProp` con DOM parcial |
| P2 — `data-precio` no asignado | Task 5, `actualizarSelectPaqueteProp` |
| P3 — `onPaqueteChangeProp` no llama `actualizarPrecio` | Task 5, `onPaqueteChangeProp` |
| P4 — `quitarPropiedad` destruye datos | Task 5, `quitarPropiedad` con `nuevoEstado` |
| P5 — Card "PROPIEDAD 1" para 1 prop | Task 8, condición `props.length === 1` |
| P6 — `actualizarPaquetesAdicionales` filtra solo prop 1 | Task 6 — comportamiento intencional documentado |
| P7 — `renderRevision` sin código concreto | Task 8 — código completo |
| P8 — Adapter sin verificación | Task 10 |
| P9 — `precioManual` vs cambio de paquete | Intencional — documentado |
| P10 — `precioManual`/`anticipoManual` no declaradas | Ya existen en línea 990-991 (verificado) |
| P11 — `actualizarAnticipo` no definida | Ya existe en línea 1507 (verificado) |
| P12 — Contratos particular huérfanos | Task 0 |
| P13 — Portal rompe UI de particulares | Task 8 — guards defensivos sobre datos |
| P14 — `firmaCliente` acepta adicionales en particulares | Task 2 + análisis: safe por diseño |
| P15 — Handler 410 en portal.js no index.js | Task 2 — en portal.js |
| P16 — Sin límite de propiedades en backend | Task 1 — `if propsData.length > 20` |
| P17 — `fechaSesion` sin validación de formato | Task 1 — regex `/^\d{4}-\d{2}-\d{2}$/` |

---

## Self-review del plan

### 1. Cobertura de objetivos del PLAN_UNIFICACION_V5.md

| Objetivo | Task que lo implementa |
|----------|----------------------|
| Eliminar "Cotización particular" | Task 1 (backend fuerza 'estandar'), Task 6 (frontend sin subtabs), Task 8 (portal sin esParticular) |
| Tipo por propiedad (Residencial/Terreno) | Task 5 (`tiposProp` per-prop) |
| Servicios libres (texto libre) | Task 6 (`crearContrato` — `paqueteVal = paqClave \|\| nombreSvc`) |
| Sin límite de 5 propiedades | Task 5 (`agregarPropiedad`, `quitarPropiedad`) |
| Adicionales para todos | Task 2 (guard eliminado en `firmaCliente`), Task 8 (guard eliminado en portal) |
| Deprecar `guardarConfiguracion` | Task 2 (410 en portal.js) |

### 2. Scan de placeholders

Ningún paso dice "TBD", "implementar después", "manejo de errores apropiado" o "similar a Task N" sin mostrar código.

### 3. Consistencia de nombres y tipos

- `numProps` / `tiposProp` declarados en Task 5, usados en Tasks 5, 6.
- `prop-nombre-servicio-N` / `prop-paquete-N` / `prop-entregables-N` / `prop-fecha-N` / `prop-hora-N` — consistentes en `renderPropCard`, `crearContrato`, `leerEstadoProps`, `restaurarEstadoProps`, `duplicarContrato`.
- `actualizarPaquetesAdicionales` — nombre consistente en `cargarPaquetes` (Task 6.1) y `limpiarFormCrear` (Task 6.5) y `setTipoProp` (Task 5).
- `paqueteBaseFinal` y `tipoPaqueteFinal` en contratos.js — solo en Task 1, no se referencian en otros archivos.
- IDs del formulario que siguen existiendo tras el cambio: `est-nombre`, `est-correo`, `est-telefono`, `est-precio-total`, `est-anticipo`, `lista-adicionales`, `campo-adicionales`, `lista-extras-acordados`, `campo-extras-catalogo`, `msg-crear`, `btn-crear`, `resultado-crear` — todos presentes en el nuevo HTML (Task 4) y referenciados en el nuevo JS (Task 6).
- IDs eliminados que ya no se referencian tras cleanup de Task 6.7: `est-paquete`, `est-fecha`, `est-hora`, `form-estandar`, `form-particular`, `par-*`, `btn-crear-estandar`, `btn-crear-particular`, `msg-estandar`, `msg-particular`, `resultado-estandar`, `resultado-particular`.
